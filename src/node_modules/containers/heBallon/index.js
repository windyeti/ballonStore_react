import React from 'react';
import { connect } from 'react-redux';

import {
    searchById
} from 'selectors';

class HeBallon extends React.Component {
    render() {
        // this.props.goodsSetId массив положить в массив из одного элемента с помощью R.of(foo),
        // так как функция renderBallon(arrArrs) принимает на вход массив массивов.
        // Ее следует вынести из catalog, чтобы она была общей
        console.log('good', this.props.goodsSetId );
        return (
            <div>HeBallon</div>
        )
    }
}
const mapStateToProps = (state, ownProps) => ({
    goodsSetId : searchById(state, ownProps)
});

export default connect(mapStateToProps, null)(HeBallon)