import React from 'react';
import { connect } from 'react-redux';
import { Link } from 'react-router';

import {
    amount,
    coast
        } from 'selectors';

class Basket extends React.Component {
    render() {
        // const amount = (arr) => {
        //     const allAmount = R.compose(
        //         R.sum,
        //         R.pluck('amount')
        //     )(arr);
        //     return (
        //         <div>
        //             Товаров { allAmount }
        //         </div>
        //     )
        // };
        // const coast = (arr) => {
        //     const sum = R.compose(
        //         R.sum,
        //         R.map(good => R.multiply(Number(good.price.split(' ')[0]), Number(good.amount)))
        //     )(arr);
        //     return (
        //         <div>
        //             На сумму { sum } руб.
        //         </div>
        //     )
        // };
        return (
            <div className="basket">
                <div className="panel panel-primary">
                    <div className="panel-heading">
                        <div className="panel-title">
                            <Link to={'/basket'}>
                                Корзина
                            </Link>
                        </div>
                    </div>
                    <div className="panel-body">
                        <div>
                            Товаров { this.props.amountAllGoods } шт.
                        </div>
                        <div>
                            На сумму { this.props.coastAllGoods } руб.
                        </div>
                    </div>
                </div>
            </div>
        )
    }
}
const mapStateToProps = state => ({
    amountAllGoods : amount(state),
    coastAllGoods : coast(state)
});
const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(Basket)
